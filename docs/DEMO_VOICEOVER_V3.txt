One classroom. Every voice. One journey.
Meet ClassTrek, the live lesson that moves with the room.
Live class pulse. Source-grounded AI. Teacher in control. Little steps. One big learning journey. Together.
Most interactive lessons collect answers, but the lesson stays fixed. ClassTrek lets student thinking safely change what happens next.
This is a working, source-grounded middle-school mission: survive one night on Mars.
The teacher remains Mission Control. They can pause the room, inspect the class pulse, and decide what is published.
Students join the same session on their own devices. They react, choose an answer, and explain their reasoning.
The shared display shows only privacy-preserving class signals, never a public speed leaderboard.
Every accepted action updates one server-authoritative, versioned classroom state.
Server-Sent Events synchronize all three surfaces, while snapshot recovery handles refreshes.
Before analysis, student text passes deterministic K through twelve checks and OpenAI moderation.
A harmful response is blocked and hidden. The teacher sees the safety decision, while the class never receives the explicit text.
Now, Mission Control asks ClassTrek to read the room.
GPT-5.6 combines de-identified reasoning, the live class pulse, and sources selected for the current journey into a structured next scene.
The server validates every source identifier. The proposal stays in teacher review, and the model cannot publish it.
Only the teacher can approve the branch.
One decision advances the canonical version, and every connected screen moves to the same explanation.
A returning student rejoins the current snapshot instead of a stale slide. This is a real multi-user lesson loop, not a scripted mock-up.
Codex shaped explicit safety and realtime contracts, then accelerated the Next.js implementation, tests, and three-browser verification.
Codex also exposed production-only failures: GPT-5.6 needed a better structured-output path, and realtime streams needed graceful rotation before the serverless limit.
I kept the key product decisions human: anonymous whole-class synthesis, grounded generation, and teacher publishing authority.
ClassTrek turns small moments of student thinking into one big learning journey. Together.
